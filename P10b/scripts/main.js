var numeros = [];

    for(i = 0; i < 2; i++){
        Math.floor((Math.random() * 100) + 1).push(numeros);
    }
var pares = [];
var impares = [];

    if(numero %2 == 0){
    push(pares);
    }else{
    push(impares);
    }
    alert(pares);
    alert(impares);
